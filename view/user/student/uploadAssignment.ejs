<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Assignment</title>
    <link rel="stylesheet" href="/css/usercss/uploadAssignment.css">
</head>

<body>
    <div class="form-container">

        <h2>Submit New Assignment</h2>
        <p class="subtitle">Please provide all necessary details and upload your file.</p>

        <form action="/student/assignment/upload" method="POST" enctype="multipart/form-data">

            <% if(success){ %>
                <div class="success-message"><%= success %></div>
            <% } %>

            <div class="input-group">
                <label for="title">Assignment Title</label>
                <input type="text" id="title" name="title" required placeholder="e.g., Final Project Report: Quantum Computing">
            </div>

            <div class="input-group">
                <label for="category">Submission Category</label>
                <select id="category" name="category" required>
                    <option value="" disabled selected>Select the type of submission</option>
                    <option value="assignment">Course Assignment</option>
                    <option value="report">Lab Report / Technical Report</option>
                    <option value="thesis">Thesis / Dissertation</option>
                    <option value="presentation">Presentation Slides</option>
                </select>
            </div>

            <div class="input-group">
                <label for="description">Description / Notes</label>
                <textarea id="description" name="description" placeholder="Add any relevant instructions, notes for the professor, or comments about late submission."></textarea>
            </div>

            <div class="input-group">
                <label for="file_upload">Upload File</label>

                <div class="file-input-wrapper">
                    <label for="file_upload" class="file-upload-label">Click here to browse files</label>
                    <p class="file-upload-info">Max file size 10MB. Accepted format: PDF.</p>
                    <input type="file" id="file_upload" name="file" required accept=".pdf">
                </div>
            </div>

            <div class="button-group">
                <button type="submit" class="submit-button">Submit Assignment</button>
                <button type="button" class="cancel-button" onclick="history.back()">Cancel</button>
                <button type="button" class="cancel-button" onclick="window.location.href='/student/dashboard'">Go to Home</button>
            </div>

        </form>
    </div>

    <script>
        const fileInput = document.getElementById("file_upload");
        const fileLabel = document.querySelector(".file-upload-label");

        fileInput.addEventListener("change", function () {
            if (this.files.length > 0) {
                fileLabel.textContent = this.files[0].name;
            } else {
                fileLabel.textContent = "Click here to browse files";
            }
        });
    </script>

</body>
</html>
