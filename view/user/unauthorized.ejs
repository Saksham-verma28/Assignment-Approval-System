<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authentication Required</title>
    <style>
        :root {
            --color-cream: #fdf8f4;
            --color-dark-brown: #5d4037;
            --color-light-brown: #8d6e63;
            --color-terracotta: #e07a5f;
            --color-light-terracotta: #d4bba2;
            --color-white: #ffffff;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: "Georgia", serif;
            background: linear-gradient(135deg, var(--color-cream) 0%, #f5ebe0 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .auth-container {
            width: 100%;
            max-width: 480px;
            background: var(--color-white);
            padding: 3rem 2.5rem;
            border-radius: 16px;
            border-top: 6px solid var(--color-terracotta);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
            text-align: center;
            animation: slideIn 0.5s ease-out;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .lock-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 1.5rem;
            background: linear-gradient(135deg, var(--color-terracotta) 0%, #d4675a 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 8px 24px rgba(224, 122, 95, 0.3);
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                box-shadow: 0 8px 24px rgba(224, 122, 95, 0.3);
            }
            50% {
                transform: scale(1.05);
                box-shadow: 0 12px 32px rgba(224, 122, 95, 0.4);
            }
        }

        .lock-icon svg {
            width: 40px;
            height: 40px;
            fill: var(--color-white);
        }

        h1 {
            font-size: 1.875rem;
            color: var(--color-dark-brown);
            margin-bottom: 1rem;
            font-weight: bold;
        }

        .message {
            font-size: 1.1rem;
            color: var(--color-light-brown);
            line-height: 1.6;
            margin-bottom: 2rem;
        }

        .info-box {
            background: var(--color-cream);
            padding: 1.25rem;
            border-radius: 12px;
            border-left: 4px solid var(--color-terracotta);
            margin-bottom: 2rem;
            text-align: left;
        }

        .info-box p {
            color: var(--color-dark-brown);
            font-size: 0.95rem;
            line-height: 1.5;
        }

        .info-box strong {
            color: var(--color-terracotta);
        }

        .button-group {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .login-btn {
            padding: 14px 32px;
            background: linear-gradient(135deg, var(--color-terracotta) 0%, #d4675a 100%);
            color: var(--color-white);
            border: none;
            border-radius: 10px;
            font-size: 1.1rem;
            font-weight: 600;
            font-family: "Georgia", serif;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 12px rgba(224, 122, 95, 0.3);
        }

        .login-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(224, 122, 95, 0.4);
            background: linear-gradient(135deg, #ff7a00 0%, var(--color-terracotta) 100%);
        }

        .login-btn:active {
            transform: translateY(0);
        }

        .secondary-btn {
            padding: 12px 32px;
            background: transparent;
            color: var(--color-terracotta);
            border: 2px solid var(--color-light-terracotta);
            border-radius: 10px;
            font-size: 1rem;
            font-weight: 600;
            font-family: "Georgia", serif;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .secondary-btn:hover {
            background: var(--color-cream);
            border-color: var(--color-terracotta);
        }

        .footer-text {
            margin-top: 2rem;
            padding-top: 1.5rem;
            border-top: 1px solid var(--color-light-terracotta);
            color: var(--color-light-brown);
            font-size: 0.9rem;
        }

        .footer-text a {
            color: var(--color-terracotta);
            text-decoration: none;
            font-weight: 600;
            transition: color 0.2s ease;
        }

        .footer-text a:hover {
            color: #ff7a00;
        }

        @media (max-width: 600px) {
            .auth-container {
                padding: 2rem 1.5rem;
            }

            h1 {
                font-size: 1.5rem;
            }

            .message {
                font-size: 1rem;
            }

            .lock-icon {
                width: 70px;
                height: 70px;
            }

            .lock-icon svg {
                width: 35px;
                height: 35px;
            }
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <div class="lock-icon">
            <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path d="M18 8h-1V6c0-2.76-2.24-5-5-5S7 3.24 7 6v2H6c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V10c0-1.1-.9-2-2-2zM9 6c0-1.66 1.34-3 3-3s3 1.34 3 3v2H9V6zm9 14H6V10h12v10zm-6-3c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2z"/>
            </svg>
        </div>

        <h1>Authentication Required</h1>
        
        <p class="message">
            You need to be logged in to access this page. Please sign in to continue.
        </p>

        <div class="info-box">
            <p>
                <strong>Why am I seeing this?</strong><br>
                Your session may have expired or you haven't logged in yet. Please authenticate to access your dashboard and submissions.
            </p>
        </div>

        <div class="button-group">
            <button class="login-btn" onclick="handleLogin()">
                Login to Continue
            </button>
            
        </div>

        <div class="footer-text">
            Need help? <a href="#contact">Contact Support</a>
        </div>
    </div>

    <script>
        function handleLogin() {
            window.location.href = '/user/login';
        }

        document.addEventListener('keydown', function(e) {
            if (e.key === 'Enter') {
                handleLogin();
            } else if (e.key === 'Escape') {
                handleGoBack();
            }
        });
    </script>
</body>
</html>